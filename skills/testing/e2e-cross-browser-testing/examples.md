# E2E Cross-Browser Testing Examples

## Responsive Dashboard

Run the primary insight journey on desktop Chromium, desktop Firefox, mobile WebKit, and a keyboard-only desktop case. Run the full chart matrix on the scheduled regression pipeline.

## Payment Form

Add Safari coverage because provider-hosted fields and storage behavior differ there. Keep unsupported legacy browsers explicit rather than silently skipping them.
