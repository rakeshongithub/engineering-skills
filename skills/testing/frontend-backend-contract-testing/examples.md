# Frontend Backend Contract Testing Examples

## Orders API

Contract-check required item fields, currency representation, idempotency errors, authorization failures, and additive response fields before provider release.

## Search API

Verify pagination cursors, empty results, filter encoding, rate-limit errors, and nullable result fields against the frontend consumer expectations.
